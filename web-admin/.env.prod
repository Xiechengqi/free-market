# Prod build points axios at same-origin /admin/api (Cloudflare → Rust binary).
VITE_SERVICE_BASE_URL=/admin/api

# Unused but expected by soybean's getServiceBaseURL helper.
VITE_OTHER_SERVICE_BASE_URL=`{
  "demo": "/admin/api"
}`
